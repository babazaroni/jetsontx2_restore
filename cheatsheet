

Add all files to git

git add --all

git commit -am "commit message"
git push

git diff --name-only

env | grep "ROS"

roscd robot_pose_ekf #takes you to the package location

rostopic list
rostopic echo /imu  # display the topic messages
